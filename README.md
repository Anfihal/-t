# Infinite Leaders Tech — 3D Mockup
Минималистичный 3D-интерфейс для Infinite Leaders Tech, созданный на базе **Spline** с полным контролем над отображением — включая мгновенное удаление вотермарки.

## 🌐 Демо

👉 [Посмотреть онлайн](https://anfihal.github.io/-t/)

## ✨ Особенности

- Полноэкранный 3D-рендер без обрезки изображения
- Адаптивный дизайн (мобильные + десктоп)
- Мгновенное скрытие вотермарки через доступ к Shadow DOM
- Чистый код без фреймворков (только нативный JS)
- Белый фон для максимальной фокусировки на контенте

## ⚙️ Как это работает

Вотермарка Spline (`<a id="logo">`) находится внутри **Shadow DOM** компонента `<hana-viewer>`. Обычный CSS не может до неё достучаться — поэтому используется оптимизированный JavaScript:

```js
// Проверка каждые 50мс → мгновенное удаление при появлении
const logo = shadowRoot.getElementById('logo');
logo?.remove();
